[project]
name = "scopeanchor-custom-loop-example"
version = "0.1.1"
description = "Minimal custom-loop host for ScopeAnchor."
requires-python = ">=3.10"
dependencies = ["scopeanchor"]

[dependency-groups]
dev = ["pyright>=1.1.400,<2", "pytest>=8.4,<9", "ruff>=0.12,<1"]

[tool.ruff]
target-version = "py310"

[tool.uv.sources]
scopeanchor = { path = "../..", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
